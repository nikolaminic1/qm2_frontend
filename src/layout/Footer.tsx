import React, { FC, ReactElement } from "react";
import { FooterDiv } from "./styleLayout";

interface FooterProps {}

const Footer: FC<FooterProps> = ({}): ReactElement => {
  return <FooterDiv>s</FooterDiv>;
};

export default Footer;
