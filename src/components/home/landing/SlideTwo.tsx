import React, { FC, ReactElement } from "react";

interface SlideTwoProps {
  slideTwoData: {};
}

const SlideTwo: FC<SlideTwoProps> = ({}): ReactElement => {
  return (
    <div>
      <img src={""} />
    </div>
  );
};

export default SlideTwo;
