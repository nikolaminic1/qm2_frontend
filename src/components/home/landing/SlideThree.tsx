import React, { FC, ReactElement } from "react";

interface SlideThreeProps {
  slideThreeData: {};
}

const SlideThree: FC<SlideThreeProps> = ({}): ReactElement => {
  return (
    <div>
      <img src={""} />
    </div>
  );
};

export default SlideThree;
